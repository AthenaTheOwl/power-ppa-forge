# Streamlit Community Cloud reads this file. The app (streamlit_app.py) reads the
# committed runs/v0_baseline/*.json directly, so it only needs streamlit. The
# CLI/package under power_ppa_forge/ has no third-party deps (pyproject.toml).
streamlit>=1.40
