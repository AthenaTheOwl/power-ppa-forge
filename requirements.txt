# Streamlit Community Cloud reads this file. The app reads the committed
# runs/v0_baseline/*.json AND drives the real engine live: the interactive
# section imports power_ppa_forge.simulator.run_simulation, so the package must
# be installable. The `.` line installs this repo as a package (see pyproject.toml).
streamlit>=1.40
.
